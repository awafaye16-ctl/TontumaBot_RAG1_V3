# ─────────────────────────────────────────────────────────────────────────────
#  TontumaBot V3 — dépendances
# ─────────────────────────────────────────────────────────────────────────────

# ── Framework ML ─────────────────────────────────────────────────────────────
torch
torchaudio
transformers==4.46.3
sentencepiece
accelerate            # requis pour device_map="auto" (LLM local)
bitsandbytes          # quantification 4bit/8bit (LLM local Qwen)
safetensors

# ── Traduction NLLB ──────────────────────────────────────────────────────────
# bilalfaye/nllb-200-distilled-600M-wo-fr-en (WO→FR et FR→WO)

# ── STT ───────────────────────────────────────────────────────────────────────
# soynade-research/Wolof-HuBERT-CTC — chargé via transformers depuis dossier local
librosa>=0.10.2       # chargement audio multi-format

# ── RAG ───────────────────────────────────────────────────────────────────────
rank-bm25
sentence-transformers
# Épinglé : chaque organisation a sa base sur le disque. Une montée de version
# majeure au prochain `pip install` peut rendre illisibles tous les index des
# clients en même temps — ce n'est plus une base de démonstration.
chromadb==1.5.9

# Lecture du stockage objet MinIO (ingestion par pointeur bucket + objectKey).
# Facultatif : sans lui, seule l'ingestion directe multipart fonctionne, et le
# service démarre normalement.
boto3
pypdf

# ── Reranker ──────────────────────────────────────────────────────────────────
# cross-encoder/ms-marco-MiniLM-L-6-v2 — téléchargé via sentence-transformers

# ── API / LLM ────────────────────────────────────────────────────────────────
fastapi
uvicorn
python-multipart
groq
google-generativeai

# ── TTS Oolel-Voices ─────────────────────────────────────────────────────────
# soynade-research/Oolel-Voices — téléchargé via huggingface_hub.snapshot_download
huggingface_hub
soundfile
s3tokenizer            # dépendance Oolel-Voices
diffusers==0.29.0     # dépendance Oolel-Voices
conformer==0.3.2      # dépendance Oolel-Voices
omegaconf             # dépendance Oolel-Voices
scipy
numpy<2.0

# ── Divers ───────────────────────────────────────────────────────────────────
langdetect
pillow
qrcode[pil]
